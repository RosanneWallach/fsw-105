var name = "John"
var age = 101

function runForLoop(pets) [
    var petObjects = []
    for (var i = 0; i < pets.length; i++) {

        var pet = { type: pets[i]}
        name;
        if (pets[i] === "cat") [
            name = "fluffy"
        ] else {
            name = "spot"
        }
        console.log("pet name: ", name)
        pet.name = namepetObkects.push(pet)
    
]
runForLoop(["cat", "dog"])